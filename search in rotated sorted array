o(n)
brute force
linear search
 int search(vector<int>& nums, int target) {
        int ans=-1;
        for(int i=0;i<nums.size();i++)
        {
            if(target==nums[i])
            {
                ans=i;
            }
        }
        return ans;
    }

o(logn)
binary search
